streamlit>=1.35.0
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
pyxlsb
streamlit-aggrid>=0.3.4

# Optional — เร่งความเร็ว snapshot (ไม่ติดตั้งก็ใช้ได้ จะ fallback เป็น csv)
# pyarrow>=15.0.0

# Optional — ติดตั้งเมื่อสลับเป็น SharePoint mode เท่านั้น
# Office365-REST-Python-Client>=2.5.0
